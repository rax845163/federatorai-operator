apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  namespace: federatorai
  name: fedemeter-api
spec:
  tls:
  - secretName: secret-tls
  rules:
  - host:
    http:
      paths:
      - path: /fedemeter-api/v1/
        backend:
          serviceName: fedemeter
          servicePort: 8999