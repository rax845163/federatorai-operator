apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: alameda-evictioner-data.pvc
  namespace: federatorai
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi 