apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-alameda.grafana-data.pvc
  namespace: federatorai
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi 