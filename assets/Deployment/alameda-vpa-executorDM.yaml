apiVersion: apps/v1
kind: Deployment
metadata:
  name: alameda-vpa-executor
  namespace: federatorai
  labels:
    app: alameda
    component: alameda-vpa-executor
  annotations:
    description: Defines how to deploy the alameda-vpa-executor
    template.alpha.openshift.io/wait-for-ready: 'true'
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      component: alameda-vpa-executor
  template:
    metadata:
      name: alameda-vpa-executor
      labels:
        app: alameda
        component: alameda-vpa-executor
    spec:
      containers:
      - name: alameda-vpa-executor
        image: quay.io/prophetstor/alameda-vpa-executor:v0.3.8
        imagePullPolicy: IfNotPresent
        resources:
          requests:
            cpu: 45m
            memory: 75Mi
        env:
        - name: NAMESPACE_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: ALAMEDA_VPA_EXECUTOR_DATAHUB_ADDRESS
          value: "alameda-datahub.federatorai.svc:50050"
        - name: ALAMEDA_VPA_EXECUTOR_ADMISSIONCONTROLLER_TLSCERTFILE
          value: /etc/tls-certs/tls.crt
        - name: ALAMEDA_VPA_EXECUTOR_ADMISSIONCONTROLLER_TLSPRIVATEKEYFILE
          value: /etc/tls-certs/tls.key
        - name: ALAMEDA_VPA_EXECUTOR_ADMISSIONCONTROLLER_CACERTFILE
          value: /etc/tls-certs/ca.crt
        volumeMounts:
        - mountPath: /etc/tls-certs
          name: tls-certs
          readOnly: false
      volumes:
      - name:  alameda-vpa-executor-data-storage
      - name:  alameda-vpa-executor-log-storage
      - name: tls-certs
        secret:
          secretName: alameda-vpa-executor-server-cert
      serviceAccount: alameda-vpa-executor
